overwrite: true
schema: "schema.graphql"
documents: "graphql/**/*.graphql"
generates:
  lib/generated/client.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
  ./graphql.schema.json:
    plugins:
      - "introspection"
